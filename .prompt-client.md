## Client UI Improvements

1. **Home page**: Show top 20 shows on load (GET /api/shows) before any search
2. **ShowCard**: Show the synopsis snippet (first 80 chars), improve visual layout
3. **ShowDetail**: Fix any field name issues, show recommendations in a better grid
4. **Watchlist**: Show "added" date, improve empty state message
5. **Add basic styling**: Dark background, card shadows, better spacing

Don't change any API calls — the api.js is correct. Just improve the UI components.
